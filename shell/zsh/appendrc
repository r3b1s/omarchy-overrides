# ----- appended ----- omarchy-overrides -----

# zsh settings
bindkey -v # vi mode

# sourcing
export RTK_TELEMETRY_DISABLED=1
source $HOME/.local/share/omarchy-overrides/shell/envs # env vars
source $DOTS/shell/aliases # aliases
source $DOTS/shell/fns # functions
