# ----- appended ----- omarchy-overrides -----

# bash settings
set -o vi # vi mode

# sourcing
source $HOME/.local/share/omarchy-overrides/shell/envs # env vars $DOTS is defined here
source $DOTS/shell/aliases # aliases
source $DOTS/shell/fns # functions
source $DOTS/shell/pi-harness # custom functionality for the pi agent harness (pi.dev)
source $DOTS/shell/other # everything else

# Terminal-specific configs (uncomment as needed)
# source $DOTS/shell/bash/ghostty
# source $DOTS/shell/bash/alacritty

# mise

eval "$(/usr/bin/mise activate bash)"
