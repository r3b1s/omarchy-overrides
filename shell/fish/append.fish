# ----- appended ----- omarchy-overrides -----

# Ghostty shell integration (ssh wrapper, terminfo, cursor, etc.)
source /usr/share/ghostty/shell-integration/fish/vendor_conf.d/ghostty-shell-integration.fish

set -gx RTK_TELEMETRY_DISABLED 1
