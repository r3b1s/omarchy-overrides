# Telemetry opt-outs — managed via bin/telem
# Format: KEY=VALUE (no export, no shell syntax)

# rtk
RTK_TELEMETRY_DISABLED=1

# mise
MISE_USE_VERSIONS_HOST_TRACK=false

# Netbird
NETBIRD_DISABLE_ANONYMOUS_METRICS=true
