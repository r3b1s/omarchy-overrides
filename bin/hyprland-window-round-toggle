#!/usr/bin/env sh
set -eu

script_path=$(readlink -f -- "$0")
root_dir=$(dirname -- "$(dirname -- "$script_path")")

hyprctl eval "package.path = [[${root_dir}/?.lua;]] .. package.path; package.loaded['hypr.actions'] = nil; require('hypr.actions').toggle_rounding()"
